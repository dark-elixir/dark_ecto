defmodule DarkEcto.Projections.CLITest do
  @moduledoc """
  Test for `DarkEcto.Projections.CLI`
  """

  use ExUnit.Case, async: true

  # alias DarkEcto.Projections.CLI

  # describe ".cast_ecto_schema/1" do
  #   test "with ExampleEctoSchema" do
  #     assert CLI.cast_ecto_schema(ExampleEctoSchema) == %{}
  #   end
  # end
end
